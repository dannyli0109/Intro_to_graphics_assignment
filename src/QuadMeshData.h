#pragma once
#include "MeshData.h"
#include <vector>
class QuadMeshData :
	public MeshData
{
public:
	/**
	 * Quad mesh, mainly use for storing the frame buffer to a quad texture.
	 * 
	 */
	QuadMeshData();
};

